<Page
    x:Class="MonocleGiraffe.Pages.FlipViewPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MonocleGiraffe.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    x:Name="FlipPage">
    <Page.TopAppBar>
        <CommandBar x:Name="TopCommandBar">
            <CommandBar.PrimaryCommands>
                <AppBarButton x:Name="ShareButton" Icon="ReShare" Label="Share"/>
                <AppBarButton x:Name="SaveButton" Icon="Save" Label="Save" />
            </CommandBar.PrimaryCommands>
        </CommandBar>
    </Page.TopAppBar>
    <Page.Resources>
        <!--<DataTemplate x:Key="ImageDataTemplate">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Text="{Binding Title}" Grid.Row="0"/>
                <TextBlock Text="{Binding Type}" Grid.Row="1"/>
                <Viewbox x:Name="ImageWrapper" StretchDirection="DownOnly" Grid.Row="1" DoubleTapped="ImageWrapper_DoubleTapped">
                    <Image Source="{Binding Link}" Stretch="None"/>
                </Viewbox>
            </Grid>
        </DataTemplate>-->
        <!--<DataTemplate x:Key="ZoomedImageDataTemplate">
            <Grid x:Name="ScrollViewerWrapper" SizeChanged="ScrollViewerWrapper_SizeChanged">
                <ScrollViewer x:Name="ZoomedImageWrapper" DoubleTapped="ZoomedImageWrapper_DoubleTapped" HorizontalScrollBarVisibility="Auto" ZoomMode="Enabled" MaxZoomFactor="1">
                    <Image Source="{Binding Link}" MaxWidth="{Binding DataContext.PageWidth, ElementName=FlipViewPage}" MaxHeight="{Binding DataContext.PageHeight, ElementName=FlipViewPage}"/>
                </ScrollViewer>
            </Grid>
        </DataTemplate>-->
    </Page.Resources>

    <Grid x:Name="LayoutRoot" Background="DimGray">
        <FlipView x:Name="MainFlipView" ItemsSource="{Binding ImageItems}" SelectionChanged="MainFlipView_SelectionChanged" SelectedIndex="{Binding SelectedIndex, Mode=TwoWay}">
            <FlipView.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <TextBlock Text="{Binding Title}" Grid.Row="0"/>
                        <TextBlock Text="{Binding Type}" Grid.Row="1"/>
                        <Grid x:Name="ScrollViewerWrapper" SizeChanged="ScrollViewerWrapper_SizeChanged" Grid.Row="1">
                            <ScrollViewer x:Name="ZoomedImageWrapper" DoubleTapped="ZoomedImageWrapper_DoubleTapped" Loaded="ZoomedImageWrapper_Loaded" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto" ZoomMode="Enabled" MinZoomFactor="{Binding DataContext.ZoomFactor, ElementName=FlipPage}" MaxZoomFactor="1">
                                <Image Source="{Binding Link}" Stretch="None" ImageOpened="Image_ImageOpened"/> <!--MaxWidth="{Binding DataContext.ViewPortWidth, ElementName=FlipPage}" MaxHeight="{Binding DataContext.ViewPortHeight, ElementName=FlipPage}"/>-->
                            </ScrollViewer>
                        </Grid>
                    </Grid>
                </DataTemplate>
            </FlipView.ItemTemplate>
        </FlipView>
    </Grid>
</Page>
