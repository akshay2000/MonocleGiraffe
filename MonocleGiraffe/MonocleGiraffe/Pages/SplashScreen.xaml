<Page
    x:Class="MonocleGiraffe.Pages.SplashScreen"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MonocleGiraffe.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:Interactivity="using:Microsoft.Xaml.Interactivity" 
    xmlns:Core="using:Microsoft.Xaml.Interactions.Core"
    mc:Ignorable="d">

    <Grid Background="Black" Loaded="Grid_Loaded">
        <StackPanel Padding="12" VerticalAlignment="Center" HorizontalAlignment="Center">
            <TextBlock Text="{Binding Message}"/>
            <ProgressRing Margin="12" IsActive="True" Visibility="{Binding State, Converter={StaticResource StateToVisibilityConverter}, ConverterParameter=Busy}"/>
            <Button x:Name="RetryButton" Content="Retry" Click="RetryButton_Click" HorizontalAlignment="Center" Visibility="{Binding State, Converter={StaticResource StateToVisibilityConverter}, ConverterParameter=Error}"/>
        </StackPanel>        
    </Grid>
</Page>
